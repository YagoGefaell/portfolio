---
import { Image } from 'astro:assets'
import profilePicture from '../assets/profile-img.jpeg'
---

<section class="flex min-h-[calc(100vh-65px)] items-center overflow-x-hidden px-6 py-20" id="about">
  <div class="container grid grid-cols-1 gap-16 md:grid-cols-2 md:items-center">
    <div>
      <p class="mb-2 text-lg font-medium text-accent-light">Hi there, I'm</p>
      <h1 class="hero-name-gradient mb-2 font-black leading-tight" style="font-size: clamp(2.4rem, 6vw, 4rem);">Yago Gefaell</h1>
      <h2 class="mb-6 font-medium text-text-muted" style="font-size: clamp(1.1rem, 2.5vw, 1.5rem);">Full Stack Developer</h2>
      <p class="mb-9 max-w-xl leading-relaxed text-text-muted">
        I build modern web applications with a passion for clean code and great user experiences.
        I enjoy working across the full stack — from interactive frontends with React and Vue
        to robust backends with Java &amp; Spring Boot, all packaged with Docker.
      </p>
      <div class="flex flex-wrap gap-4">
        <a href="#projects" class="btn btn-primary">View Projects</a>
        <a href="#contact" class="btn btn-outline">Get in Touch</a>
      </div>
    </div>
    <div class="relative flex items-center justify-center md:h-96" aria-hidden="true">
      <div class="avatar-ring">
        <Image
          src={profilePicture}
          alt="Yago Gefaell picture"
          width={320}
          height={320}
          loading="eager"
        />
      </div>
      <div class="blob blob-1"></div>
      <div class="blob blob-2"></div>
    </div>
  </div>
</section>
